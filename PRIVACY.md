# データ処理とプライバシー

## 処理の流れ

1. 利用者が端末上の `.docx` または `.txt` を選択します。
2. ブラウザのメモリ内で本文を抽出します。
3. ルールベースで発話者と意味単位の候補を作ります。
4. 利用者が画面上で修正します。
5. ブラウザのメモリ内で `.xlsx` を生成し、端末へ保存します。

この過程で、文書内容をサーバー、AIモデル、外部APIへ送信しません。

## 技術的な制約

- 単一HTMLに必要なコードをすべて内包し、外部スクリプトやCDNを読み込みません。
- Content Security Policyで `connect-src 'none'` を指定し、HTTP通信、WebSocket通信などを禁止します。
- `fetch`、`XMLHttpRequest`、`WebSocket`、`sendBeacon`を使用しません。
- 文書内容をLocalStorage、IndexedDB、Cookieへ保存しません。
- 文書内容はページを閉じるとメモリから破棄されます。
- AIモデルを使用せず、句読点、接続表現、発話者の切り替わり、文章長だけで分割候補を作ります。

## 運用上の注意

- 配布されたHTMLファイルを研究用端末に保存し、オフラインで開く運用を推奨します。
- ブラウザ拡張機能、OSのバックアップ・同期、マルウェアなど、ツール外の環境までは制御できません。
- 研究計画、倫理審査、所属機関の情報管理規程がある場合は、それらを優先してください。
- 自動分割は分析結果ではありません。最終的な意味単位は研究者が確認してください。
